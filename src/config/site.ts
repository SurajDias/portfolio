/**
 * Public URL for the production resume. `BASE_URL` keeps this valid when the
 * app is deployed at a subpath as well as at the Vercel site root.
 */
export const RESUME_URL = `${import.meta.env.BASE_URL}resume/Suraj_Dias_Resume.pdf`;

/** Public LinkedIn profile for the portfolio owner. */
export const LINKEDIN_URL = "https://linkedin.com/in/thesurajdias";

/** Public email address for the portfolio owner. */
export const EMAIL = "diassuraj13@gmail.com";
