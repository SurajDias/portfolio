import { motion } from "framer-motion";
import Container from "../components/common/Container";
import SectionHeading from "../components/ui/SectionHeading";
const milestones = [["01", "Start with the system", "Before implementation, I map the actors, failure modes, data paths, and decisions that matter."], ["02", "Make complexity legible", "Good architecture should help people reason about change—not become a barrier to it."], ["03", "Ship, observe, iterate", "I value a tight loop between what we build, what people experience, and what the system tells us."]];
export default function About() { return <section id="about" className="py-24 sm:py-32"><Container><div className="grid gap-14 lg:grid-cols-[.8fr_1.2fr]"><SectionHeading eyebrow="How I work" title="Engineering is a discipline of intentional trade-offs." description="I’m Suraj Dias, a software engineer drawn to the intersection of thoughtful product design and dependable technical systems." /><div className="border-l border-white/[.1]">{milestones.map(([number, title, text], i) => <motion.div key={number} initial={{ opacity: 0, y: 15 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * .08 }} className="relative pl-8 pb-10 last:pb-0"><span className="absolute -left-[5px] top-1.5 h-2.5 w-2.5 rounded-full border-2 border-[#050816] bg-sky-400"/><p className="text-[10px] font-medium tracking-[.18em] text-sky-300">{number}</p><h3 className="mt-2 text-lg font-medium text-slate-100">{title}</h3><p className="mt-2 max-w-xl text-sm leading-6 text-slate-400">{text}</p></motion.div>)}</div></div></Container></section>; }
